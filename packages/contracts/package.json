{
  "name": "@mcrl/contracts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./seed-history": "./src/seed-history.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint . --report-unused-disable-directives --max-warnings 0",
    "format": "bun run --cwd ../.. prettier --write packages/contracts",
    "format:check": "bun run --cwd ../.. prettier --check packages/contracts"
  },
  "dependencies": {
    "zod": "catalog:"
  }
}
